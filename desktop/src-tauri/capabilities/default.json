{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:default",
    "dialog:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        { "name": "bin/duscan", "sidecar": true, "args": true },
        { "name": "bin/dusum", "sidecar": true, "args": true },
        { "name": "bin/dudb", "sidecar": true, "args": true }
      ]
    }
  ]
}
